# Sitemap 1000

The script is made to cut sitemaps.xml into 1000 URLs chunks. If you submit those chunks separately into Google Search Console (i.e. using GSC API) you can follow index coverage of every single URL in these chunks.
